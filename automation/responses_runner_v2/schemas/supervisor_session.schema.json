{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Responses Runner V2 Supervisor Session",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "supervisor_session_id",
    "created_at",
    "updated_at",
    "workspace_root",
    "operator_boundary",
    "status",
    "current_phase",
    "clarified_task_brief",
    "policy",
    "model_defaults",
    "retry_budget",
    "monitoring_policy",
    "scaffold_versions",
    "dry_run_validations",
    "runs",
    "stage_outcomes",
    "monitoring_events",
    "review_cycles",
    "review_agent_invocations",
    "consolidations",
    "operator_acceptance_records",
    "human_pauses",
    "archives",
    "approved_review_bundles",
    "final_bundle",
    "validation_results",
    "command_log",
    "errors"
  ],
  "properties": {
    "schema_version": {
      "const": "responses_runner_v2.supervisor_session.v1",
      "description": "Schema version identifier for this artifact contract."
    },
    "supervisor_session_id": {
      "$ref": "#/$defs/idString",
      "description": "Stable identifier for the supervisor session."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when this artifact was created."
    },
    "updated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when this artifact was last updated."
    },
    "workspace_root": {
      "type": "string",
      "minLength": 1,
      "description": "Absolute workspace root recorded for local supervisor auditability."
    },
    "operator_boundary": {
      "const": "headless_discrete_codex_exec_with_deterministic_supervisor_cli",
      "description": "Named boundary separating operator-agent work from deterministic supervisor logic."
    },
    "status": {
      "enum": [
        "created",
        "clarified",
        "scaffold_staged",
        "scaffold_reviewing",
        "ready_to_launch",
        "stage_running",
        "waiting_for_review",
        "blocked",
        "completed",
        "failed"
      ],
      "description": "Current lifecycle status for this artifact."
    },
    "current_phase": {
      "enum": [
        "clarification",
        "scaffold",
        "scaffold_review",
        "stage_execution",
        "monitoring",
        "classification",
        "review",
        "acceptance",
        "bundle_creation",
        "finalization",
        "recovery"
      ],
      "description": "Current supervisor phase for this session."
    },
    "clarified_task_brief": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "sha256",
        "accepted_at",
        "summary"
      ],
      "properties": {
        "path": {
          "$ref": "#/$defs/pathString",
          "description": "Describes the `path` field for this artifact contract."
        },
        "sha256": {
          "$ref": "#/$defs/sha256",
          "description": "Describes the `sha256` field for this artifact contract."
        },
        "accepted_at": {
          "type": "string",
          "format": "date-time",
          "description": "Describes the `accepted_at` field for this artifact contract."
        },
        "summary": {
          "type": "string",
          "minLength": 1,
          "description": "Concise human-readable summary of this artifact."
        }
      },
      "description": "Accepted clarified task brief that authorizes the supervisor session."
    },
    "policy": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "review_agents",
        "retry_budgets",
        "monitoring",
        "human_pause_rules",
        "read_only_enforcement_method"
      ],
      "description": "Supervisor policy settings for review, monitoring, retry, and pause behavior."
    },
    "model_defaults": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "primary",
        "structural",
        "prompt_cache_retention",
        "max_output_tokens"
      ],
      "properties": {
        "primary": {
          "const": "gpt-5.5-pro",
          "description": "Describes the `primary` field for this artifact contract."
        },
        "structural": {
          "const": "gpt-5.5",
          "description": "Describes the `structural` field for this artifact contract."
        },
        "prompt_cache_retention": {
          "const": "24h",
          "description": "Describes the `prompt_cache_retention` field for this artifact contract."
        },
        "max_output_tokens": {
          "const": 128000,
          "description": "Describes the `max_output_tokens` field for this artifact contract."
        },
        "primary_reasoning_effort": {
          "enum": [
            "xhigh"
          ],
          "description": "Describes the `primary_reasoning_effort` field for this artifact contract."
        },
        "structural_reasoning_effort": {
          "enum": [
            "medium",
            "high",
            "xhigh"
          ],
          "description": "Describes the `structural_reasoning_effort` field for this artifact contract."
        },
        "primary_verbosity": {
          "enum": [
            "high"
          ],
          "description": "Describes the `primary_verbosity` field for this artifact contract."
        },
        "structural_verbosity": {
          "enum": [
            "medium",
            "high"
          ],
          "description": "Describes the `structural_verbosity` field for this artifact contract."
        }
      },
      "description": "Model defaults the supervisor expects generated workflows to follow."
    },
    "retry_budget": {
      "type": "object",
      "additionalProperties": true,
      "description": "Remaining retry budget for controlled recovery paths."
    },
    "monitoring_policy": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "poll_interval_seconds",
        "stale_after_seconds",
        "max_refresh_attempts",
        "max_resume_attempts",
        "no_duplicate_submit"
      ],
      "description": "Policy controlling live-run monitoring and stale-run anomaly handling."
    },
    "scaffold_versions": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/scaffoldVersion"
      },
      "description": "Scaffold versions staged for dry-run and review."
    },
    "dry_run_validations": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/dryRunValidation"
      },
      "description": "Dry-run validation records for staged scaffolds."
    },
    "runs": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/runRef"
      },
      "description": "Describes the `runs` field for this artifact contract."
    },
    "stage_outcomes": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/stageOutcomeRef"
      },
      "description": "Classified outcomes for supervised workflow stages."
    },
    "monitoring_events": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/monitoringEvent"
      },
      "description": "Monitoring events and anomalies recorded by the supervisor."
    },
    "review_cycles": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/reviewCycle"
      },
      "description": "Review cycles created for scaffolds, stages, final packets, or recovery."
    },
    "review_agent_invocations": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/agentInvocation"
      },
      "description": "Agent invocation records captured by the supervisor."
    },
    "consolidations": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/consolidationRef"
      },
      "description": "Deterministic consolidation records for reviewer outputs."
    },
    "operator_acceptance_records": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/pathString"
      },
      "description": "Operator selective-acceptance records with applied-change evidence."
    },
    "human_pauses": {
      "type": "array",
      "items": {
        "type": "object"
      },
      "description": "Human-pause artifacts created by the supervisor."
    },
    "archives": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/archiveRef"
      },
      "description": "Archive records for failed attempts or recovery evidence."
    },
    "approved_review_bundles": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/reviewBundleRef"
      },
      "description": "Approved review bundles created by this supervisor session."
    },
    "final_bundle": {
      "anyOf": [
        {
          "$ref": "#/$defs/finalBundleRef"
        },
        {
          "type": "null"
        }
      ],
      "description": "Final implementation bundle emitted by the supervisor, when available."
    },
    "validation_results": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/validationResult"
      },
      "description": "Validation results recorded against this supervisor session."
    },
    "command_log": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/commandLogEntry"
      },
      "description": "Deterministic command log entries for supervisor operations."
    },
    "errors": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/errorEntry"
      },
      "description": "Supervisor errors that require attention or recovery."
    }
  },
  "$defs": {
    "idString": {
      "type": "string",
      "pattern": "^[a-z0-9][a-z0-9._-]{0,127}$"
    },
    "pathString": {
      "type": "string",
      "minLength": 1
    },
    "sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$"
    },
    "scaffoldVersion": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "version_id",
        "path",
        "hash_manifest_path",
        "created_by",
        "dry_run_artifacts",
        "approval_status"
      ],
      "properties": {
        "version_id": {
          "$ref": "#/$defs/idString",
          "description": "Describes the `version_id` field for this artifact contract."
        },
        "path": {
          "$ref": "#/$defs/pathString",
          "description": "Describes the `path` field for this artifact contract."
        },
        "hash_manifest_path": {
          "$ref": "#/$defs/pathString",
          "description": "Describes the `hash_manifest_path` field for this artifact contract."
        },
        "created_by": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `created_by` field for this artifact contract."
        },
        "dry_run_artifacts": {
          "type": "array",
          "description": "Describes the `dry_run_artifacts` field for this artifact contract."
        },
        "approval_status": {
          "enum": [
            "staged",
            "dry_run_passed",
            "reviewing",
            "accepted",
            "blocked",
            "rejected"
          ],
          "description": "Describes the `approval_status` field for this artifact contract."
        }
      }
    },
    "dryRunValidation": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "command",
        "exit_code",
        "status"
      ],
      "properties": {
        "command": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Command metadata for the agent invocation that produced this artifact."
        },
        "exit_code": {
          "type": "integer",
          "description": "Describes the `exit_code` field for this artifact contract."
        },
        "status": {
          "enum": [
            "passed",
            "failed"
          ],
          "description": "Current lifecycle status for this artifact."
        }
      }
    },
    "runRef": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "run_id",
        "workflow_id",
        "run_dir",
        "run_manifest_path",
        "status"
      ]
    },
    "stageOutcomeRef": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "run_id",
        "stage_id",
        "classification",
        "artifact_path",
        "reviewability"
      ]
    },
    "monitoringEvent": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "timestamp",
        "run_id",
        "stage_id",
        "status",
        "action",
        "artifact_path"
      ]
    },
    "reviewCycle": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "review_cycle_id",
        "review_kind",
        "artifacts_reviewed",
        "operator_provisional_record",
        "review_agent_outputs",
        "consolidation",
        "acceptance_status"
      ]
    },
    "agentInvocation": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "command_id",
        "actor_role",
        "argv",
        "cwd",
        "started_at",
        "completed_at",
        "exit_code",
        "stdout_path",
        "stderr_path"
      ]
    },
    "consolidationRef": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "review_cycle_id",
        "consolidated_review_json",
        "consolidated_review_md",
        "recommendation_counts"
      ]
    },
    "archiveRef": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "archive_manifest_path"
      ]
    },
    "reviewBundleRef": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "bundle_path",
        "source_run_id",
        "source_stage_id",
        "artifact_hashes",
        "validation_status"
      ]
    },
    "finalBundleRef": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "bundle_path",
        "schema_validation_status",
        "file_inventory_hash",
        "acceptance_record_id"
      ],
      "properties": {
        "bundle_path": {
          "$ref": "#/$defs/pathString",
          "description": "Describes the `bundle_path` field for this artifact contract."
        },
        "schema_validation_status": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `schema_validation_status` field for this artifact contract."
        },
        "file_inventory_hash": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `file_inventory_hash` field for this artifact contract."
        },
        "acceptance_record_id": {
          "type": "string",
          "description": "Describes the `acceptance_record_id` field for this artifact contract."
        }
      }
    },
    "validationResult": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "check_id",
        "command_or_method",
        "phase",
        "expected_result",
        "actual_result",
        "status",
        "artifact_path"
      ]
    },
    "commandLogEntry": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "command_id",
        "command_type",
        "argv",
        "cwd",
        "started_at",
        "completed_at",
        "exit_code",
        "stdout_path",
        "stderr_path"
      ]
    },
    "errorEntry": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "error_id",
        "severity",
        "message",
        "related_artifact",
        "recovery_action"
      ],
      "properties": {
        "error_id": {
          "$ref": "#/$defs/idString",
          "description": "Describes the `error_id` field for this artifact contract."
        },
        "severity": {
          "enum": [
            "critical",
            "blocking",
            "high",
            "medium",
            "low"
          ],
          "description": "Severity level for a pause, issue, or recommendation."
        },
        "message": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `message` field for this artifact contract."
        },
        "related_artifact": {
          "type": "string",
          "description": "Describes the `related_artifact` field for this artifact contract."
        },
        "recovery_action": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `recovery_action` field for this artifact contract."
        }
      }
    }
  },
  "description": "Responses Runner V2 Supervisor Session JSON schema."
}
