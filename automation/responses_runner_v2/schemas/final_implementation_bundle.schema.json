{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Responses Runner V2 Final Implementation Bundle",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "packet_version",
    "created_at",
    "summary",
    "file_inventory",
    "emitted_files",
    "validation_evidence",
    "agent_reviews",
    "consolidation",
    "operator_acceptance",
    "model_migration_summary",
    "failure_policy_summary",
    "human_pause_summary",
    "rollout_instructions",
    "residual_risks"
  ],
  "properties": {
    "schema_version": {
      "const": "responses_runner_v2.final_implementation_bundle.v1",
      "description": "Schema version identifier for this artifact contract."
    },
    "packet_version": {
      "type": "string",
      "minLength": 1,
      "description": "Version identifier for the emitted implementation packet."
    },
    "created_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 timestamp when this artifact was created."
    },
    "summary": {
      "type": "string",
      "minLength": 1,
      "description": "Concise human-readable summary of this artifact."
    },
    "file_inventory": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/fileInventoryItem"
      },
      "description": "Inventory of files expected or emitted by a final implementation packet."
    },
    "emitted_files": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/emittedFile"
      },
      "description": "Files emitted by a final implementation packet."
    },
    "validation_evidence": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/validationEvidence"
      },
      "description": "Evidence proving the emitted packet was validated."
    },
    "agent_reviews": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "operator_codex",
        "codex_review_agent",
        "claude_review_agent"
      ],
      "properties": {
        "operator_codex": {
          "$ref": "#/$defs/reviewRef",
          "description": "Describes the `operator_codex` field for this artifact contract."
        },
        "codex_review_agent": {
          "$ref": "#/$defs/reviewRef",
          "description": "Describes the `codex_review_agent` field for this artifact contract."
        },
        "claude_review_agent": {
          "$ref": "#/$defs/reviewRef",
          "description": "Describes the `claude_review_agent` field for this artifact contract."
        }
      },
      "description": "Review-agent outputs considered for the final packet."
    },
    "consolidation": {
      "$ref": "#/$defs/reviewRef",
      "description": "Consolidated review findings and disposition summary."
    },
    "operator_acceptance": {
      "$ref": "#/$defs/reviewRef",
      "description": "Operator selective-acceptance record for final recommendations."
    },
    "model_migration_summary": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "primary_generation_model",
        "structural_processing_model",
        "prompt_cache_retention",
        "surfaces_updated"
      ],
      "properties": {
        "primary_generation_model": {
          "const": "gpt-5.5-pro",
          "description": "Describes the `primary_generation_model` field for this artifact contract."
        },
        "structural_processing_model": {
          "const": "gpt-5.5",
          "description": "Describes the `structural_processing_model` field for this artifact contract."
        },
        "prompt_cache_retention": {
          "const": "24h",
          "description": "Describes the `prompt_cache_retention` field for this artifact contract."
        },
        "surfaces_updated": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "description": "Describes the `surfaces_updated` field for this artifact contract."
        }
      },
      "description": "Summary of model-default and workflow posture migration."
    },
    "failure_policy_summary": {
      "type": "array",
      "minItems": 6,
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": [
          "case_id",
          "action"
        ],
        "properties": {
          "case_id": {
            "enum": [
              "completed_complete_artifact",
              "failed_complete_artifact",
              "failed_no_artifact",
              "incomplete_output_limit",
              "blocked_token_preflight",
              "long_running_monitoring_anomaly"
            ],
            "description": "Describes the `case_id` field for this artifact contract."
          },
          "action": {
            "type": "string",
            "minLength": 1,
            "description": "Recommended recovery, review, or continuation action."
          }
        }
      },
      "description": "Summary of failure-classification and recovery policy behavior."
    },
    "human_pause_summary": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": true,
        "required": [
          "trigger",
          "artifact_to_present",
          "decision_required",
          "safe_continuation_action"
        ]
      },
      "description": "Summary of human-pause conditions and artifacts."
    },
    "rollout_instructions": {
      "type": "string",
      "minLength": 1,
      "description": "Instructions for applying or operating the final bundle."
    },
    "residual_risks": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Known risks that remain after validation."
    }
  },
  "$defs": {
    "fileInventoryItem": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "action",
        "category",
        "purpose"
      ],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `path` field for this artifact contract."
        },
        "action": {
          "enum": [
            "create",
            "update"
          ],
          "description": "Recommended recovery, review, or continuation action."
        },
        "category": {
          "enum": [
            "engine",
            "task_pack",
            "docs",
            "tests",
            "config",
            "ops"
          ],
          "description": "Describes the `category` field for this artifact contract."
        },
        "purpose": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `purpose` field for this artifact contract."
        }
      }
    },
    "emittedFile": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path",
        "action",
        "content_kind",
        "sha256"
      ],
      "properties": {
        "path": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `path` field for this artifact contract."
        },
        "action": {
          "enum": [
            "create",
            "update"
          ],
          "description": "Recommended recovery, review, or continuation action."
        },
        "content_kind": {
          "enum": [
            "full_file",
            "patch"
          ],
          "description": "Describes the `content_kind` field for this artifact contract."
        },
        "sha256": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$",
          "description": "Describes the `sha256` field for this artifact contract."
        }
      }
    },
    "validationEvidence": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "check_id",
        "phase",
        "command_or_method",
        "expected_result",
        "actual_result",
        "status"
      ],
      "properties": {
        "check_id": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `check_id` field for this artifact contract."
        },
        "phase": {
          "enum": [
            "red",
            "green"
          ],
          "description": "Describes the `phase` field for this artifact contract."
        },
        "command_or_method": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `command_or_method` field for this artifact contract."
        },
        "expected_result": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `expected_result` field for this artifact contract."
        },
        "actual_result": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `actual_result` field for this artifact contract."
        },
        "status": {
          "enum": [
            "passed",
            "failed",
            "blocked"
          ],
          "description": "Current lifecycle status for this artifact."
        }
      }
    },
    "reviewRef": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "json_path",
        "markdown_path",
        "decision"
      ],
      "properties": {
        "json_path": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `json_path` field for this artifact contract."
        },
        "markdown_path": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `markdown_path` field for this artifact contract."
        },
        "decision": {
          "type": "string",
          "minLength": 1,
          "description": "Describes the `decision` field for this artifact contract."
        }
      }
    }
  },
  "description": "Responses Runner V2 Final Implementation Bundle JSON schema."
}
