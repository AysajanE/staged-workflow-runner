# Stage 3 - Execution Row Draft

Draft candidate plan-orchestrator execution rows from the reviewed source authority and repo grounding.

Output must include:

1. candidate rows with the exact `markdown_playbook_v1` table columns, including `required_verification_commands`;
2. a short rationale for each row's ordering and prerequisites;
3. row-specific repo surfaces and deliverables;
4. row-specific allowed write roots;
5. row-specific verification commands or concrete docs-only checks in the `required_verification_commands` table cell;
6. row-specific manual-gate requirements.

Do not claim the draft validates. The next stage reviews contract compliance and safety.
