# Synthetic Primary Brief

Goal: validate that the Responses Runner v2 package can execute a small workflow with clear authority order, explicit artifacts, and optional structured output.

Priority rules:

1. Prefer the primary brief over all other attached roles.
2. Preserve reviewed handoff decisions when they are supplied.
3. Use repository facts for current package details.
4. Treat reference context as lower-authority guidance only.

Expected deliverable shape:

- a concise assessment
- a small set of supporting points
- explicit open questions instead of invented certainty